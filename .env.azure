# Azure App Service Configuration
# This file contains environment-specific settings for Azure

# Python version (Azure will use this)
PYTHON_VERSION=3.11

# Enable detailed logging (helpful for debugging)
WEBSITES_ENABLE_APP_SERVICE_STORAGE=false

# Startup command (optional - can also be set in Azure Portal)
# STARTUP_COMMAND=gunicorn --bind=0.0.0.0 --timeout 600 wsgi:app
